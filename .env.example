# Example .env file with per-stage LLM configuration
# Copy this file to .env and fill in your API keys/ models/ base urls
# They can be any from any openai compatible provider or local tools like llama.cpp or LM Studio.

# ============= Skills Extraction =============
# LLM for extracting skills from job descriptions
SKILLS_EXTRACTION_MODEL= gpt-4.1-nano
SKILLS_EXTRACTION_API_KEY=sk-...
SKILLS_EXTRACTION_BASE_URL=https://api.openai.com/v1

# ============= Skills Matching =============
# LLM for matching job skills against candidate skills
SKILLS_MATCHING_MODEL= ...
SKILLS_MATCHING_API_KEY= ...
SKILLS_MATCHING_BASE_URL= ...

# ============= Job Scoring =============
# LLM for scoring job postings against resume (existing JobMatcher)
JOB_SCORING_MODEL= ...
JOB_SCORING_API_KEY= ...
JOB_SCORING_BASE_URL= ...


# ============= Chat =============
# LLM for multi-turn chat about jobs
CHAT_MODEL=Qwen3 8B
CHAT_API_KEY= ...
CHAT_BASE_URL='http://localhost:8000/v1'